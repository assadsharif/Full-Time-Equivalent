# This directory contains archived and compressed log files (P2)
# Logs older than the retention period are automatically moved here and compressed
# Compression format: gzip (configurable in config/logging.yaml)
