<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8" />
<style>
  body {
    font-family: Arial, sans-serif;
    color: #333;
    max-width: 600px;
    margin: 0 auto;
    padding: 20px;
  }
  .header {
    background: #0066CC;
    color: #fff;
    padding: 20px 24px;
    border-radius: 4px 4px 0 0;
  }
  .header h1 { margin: 0; font-size: 20px; }
  .header p  { margin: 4px 0 0; font-size: 14px; opacity: 0.9; }
  .body {
    border: 1px solid #ddd;
    border-top: none;
    padding: 24px;
    border-radius: 0 0 4px 4px;
  }
  .highlight {
    background: #f4f8fc;
    border-left: 3px solid #0066CC;
    padding: 6px 12px;
    margin: 4px 0;
    font-size: 14px;
  }
  .footer {
    color: #999;
    font-size: 12px;
    margin-top: 24px;
    border-top: 1px solid #eee;
    padding-top: 12px;
  }
</style>
</head>
<body>
  <div class="header">
    <h1>Monday Morning CEO Briefing</h1>
    <p>Week of {{ period_start }} &ndash; {{ period_end }}</p>
  </div>

  <div class="body">
    <p>Hello,</p>
    <p>Attached is your weekly AI Employee performance summary.</p>

    {% if highlights %}
    <p><strong>Key highlights:</strong></p>
    {% for h in highlights %}
    <div class="highlight">{{ h }}</div>
    {% endfor %}
    {% endif %}

    <p>Please review the full briefing for insights and recommendations.</p>
  </div>

  <div class="footer">
    <p>Generated by AI Employee (FTE v1.0) &mdash; {{ generated_at }}</p>
    <p>Attachment: {{ attachment_name }}</p>
  </div>
</body>
</html>
