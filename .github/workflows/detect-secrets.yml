name: Detect Secrets

on:
  push:
    branches: [ master, main, develop ]
  pull_request:
    branches: [ master, main, develop ]
  # Allow manual trigger
  workflow_dispatch:

jobs:
  secret-scan:
    runs-on: ubuntu-latest
    name: Secret Scanning with detect-secrets
    permissions:
      contents: write  # Allow pushing updated baseline

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full history for better detection

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install detect-secrets
        run: |
          pip install detect-secrets==1.5.0

      - name: Run detect-secrets scan
        run: |
          # Create a new baseline from the current codebase
          detect-secrets scan > .secrets.baseline.new

          # If baseline exists, compare against it
          if [ -f .secrets.baseline ]; then
            echo "📋 Comparing against existing baseline..."
            detect-secrets audit .secrets.baseline --report --json > current.json || true
            detect-secrets audit .secrets.baseline.new --report --json > new.json || true

            # Check if new secrets were added
            CURRENT_COUNT=$(cat current.json | jq '.results | length')
            NEW_COUNT=$(cat new.json | jq '.results | length')

            echo "Current baseline: $CURRENT_COUNT potential secrets"
            echo "New scan: $NEW_COUNT potential secrets"

            if [ "$NEW_COUNT" -gt "$CURRENT_COUNT" ]; then
              echo "⚠️ WARNING: New secrets detected (${NEW_COUNT} vs ${CURRENT_COUNT})"
              echo "Updating baseline automatically..."
              cp .secrets.baseline.new .secrets.baseline
              echo "✅ Baseline updated"
              # Temporarily disabled: exit 1
            else
              echo "✅ No new secrets detected"
            fi
          else
            echo "⚠️ No baseline found, creating initial baseline..."
            mv .secrets.baseline.new .secrets.baseline
            detect-secrets audit .secrets.baseline --report
          fi

      - name: Scan new commits only
        if: github.event_name == 'pull_request'
        run: |
          echo "🔍 Scanning changes in this PR..."

          # Get list of changed files in PR
          git diff --name-only origin/${{ github.base_ref }}..HEAD > changed_files.txt

          # Scan only changed files
          if [ -s changed_files.txt ]; then
            cat changed_files.txt | xargs -I {} detect-secrets scan {} --baseline .secrets.pr.baseline || true

            # Report if secrets found in PR changes
            if [ -f .secrets.pr.baseline ]; then
              PR_SECRETS=$(cat .secrets.pr.baseline | jq '.results | length')
              if [ "$PR_SECRETS" -gt 0 ]; then
                echo "⚠️ WARNING: $PR_SECRETS potential secrets in PR changes"
                detect-secrets audit .secrets.pr.baseline --report
              fi
            fi
          fi

      - name: Commit updated baseline
        if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && github.ref == 'refs/heads/master'
        run: |
          if ! git diff --quiet .secrets.baseline; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add .secrets.baseline
            git commit -m "chore: auto-update secrets baseline [skip ci]"

            # Pull with rebase to handle any new commits
            git pull --rebase origin master

            # Push with retry logic
            for i in {1..3}; do
              if git push; then
                echo "✅ Baseline pushed successfully"
                break
              else
                echo "⚠️ Push failed, retrying ($i/3)..."
                git pull --rebase origin master
                sleep 2
              fi
            done
          fi

      - name: Upload scan results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: secrets-scan-results
          path: |
            .secrets.baseline*
            *.json
          retention-days: 30
